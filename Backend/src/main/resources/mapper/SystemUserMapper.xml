<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.yoyuen.backend.mapper.SystemUserMapper">

    <!-- 结果映射：用户及其角色和权限 -->
    <resultMap id="UserWithRolesAndPermissions" type="com.yoyuen.backend.model.entity.user.SystemUserImpl">
        <id property="id" column="user_id"/>
        <result property="username" column="username"/>
        <result property="password" column="password"/>
        <result property="createTime" column="create_time"/>
        <result property="updateTime" column="update_time"/>
        <result property="deleted" column="deleted"/>

        <!-- 角色集合 -->
        <collection property="roles" ofType="com.yoyuen.backend.model.entity.user.SystemRole">
            <id property="id" column="role_id"/>
            <result property="name" column="role_name"/>
            <result property="description" column="role_description"/>
        </collection>

        <!-- 权限集合 -->
        <collection property="permissions" ofType="com.yoyuen.backend.model.entity.user.SystemPermission">
            <id property="id" column="permission_id"/>
            <result property="name" column="permission_name"/>
            <result property="description" column="permission_description"/>
        </collection>
    </resultMap>

    <!-- 查询用户及其角色和权限 -->
    <select id="getUserWithRolesAndPermissions" resultMap="UserWithRolesAndPermissions">
        SELECT
            u."id" as user_id,
            u."username",
            u."password",
            u."create_time",
            u."update_time",
            u."deleted",
            r."id" as role_id,
            r."name" as role_name,
            r."description" as role_description,
            p."id" as permission_id,
            p."name" as permission_name,
            p."description" as permission_description
        FROM "system_user" u
        LEFT JOIN "system_user_role" ur ON u."id" = ur."user_id"
        LEFT JOIN "system_role" r ON ur."role_id" = r."id"
        LEFT JOIN "system_role_permission" rp ON r."id" = rp."role_id"
        LEFT JOIN "system_permission" p ON rp."permission_id" = p."id"
        WHERE u."username" = #{name}
        AND u."deleted" = false
    </select>

</mapper>
