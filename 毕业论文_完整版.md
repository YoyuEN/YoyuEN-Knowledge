# 基于RAG技术的智能知识库问答系统的设计与实现

**学生姓名：** [你的姓名]
**学号：** [你的学号]
**专业：** 软件工程
**指导教师：** [导师姓名]
**完成日期：** 2026年3月

---

## 摘要

随着人工智能技术的快速发展，智能问答系统在知识管理和信息检索领域发挥着越来越重要的作用。传统的问答系统往往依赖于预训练的大语言模型，但存在知识更新滞后、无法获取特定领域知识等问题。检索增强生成（Retrieval-Augmented Generation，RAG）技术通过结合向量检索和大语言模型，有效解决了这些问题。

本文设计并实现了一个基于RAG技术的智能知识库问答系统。系统采用前后端分离架构，后端基于Spring Boot 3框架，集成Spring AI、MyBatis Plus、Spring Security等技术；前端采用Vue 3框架，结合Element Plus和Naive UI组件库。系统使用PostgreSQL的PgVector扩展作为向量数据库，MinIO作为对象存储，Redis作为缓存层，实现了高效的文档检索和智能问答功能。

系统的核心功能包括：知识库管理、文档向量化、RAG智能问答、内容管理、评论系统等。通过Apache Tika实现多格式文档解析，使用TokenTextSplitter进行文本分割，结合OpenAI Embedding模型生成文档向量。在问答过程中，系统首先通过向量相似度检索相关文档片段，然后将检索结果作为上下文提供给大语言模型，生成准确且具有引用来源的回答。

测试结果表明，系统能够准确理解用户问题，检索相关文档，并生成高质量的回答。系统具有良好的可扩展性和用户体验，为知识管理和智能问答提供了有效的解决方案。

**关键词：** 检索增强生成；向量数据库；智能问答；知识库管理；Spring AI；Vue 3

---

## Abstract

With the rapid development of artificial intelligence technology, intelligent question-answering systems play an increasingly important role in knowledge management and information retrieval. Traditional Q&A systems often rely on pre-trained large language models, but suffer from outdated knowledge and inability to access domain-specific information. Retrieval-Augmented Generation (RAG) technology effectively addresses these issues by combining vector retrieval with large language models.

This paper designs and implements an intelligent knowledge base Q&A system based on RAG technology. The system adopts a front-end and back-end separation architecture. The back-end is based on the Spring Boot 3 framework, integrating Spring AI, MyBatis Plus, Spring Security, and other technologies. The front-end uses the Vue 3 framework, combined with Element Plus and Naive UI component libraries. The system uses PostgreSQL's PgVector extension as the vector database, MinIO as object storage, and Redis as the caching layer, achieving efficient document retrieval and intelligent Q&A functions.

The core functions of the system include: knowledge base management, document vectorization, RAG intelligent Q&A, content management, and comment system. Multi-format document parsing is implemented through Apache Tika, text segmentation using TokenTextSplitter, and document vectors are generated using the OpenAI Embedding model. During the Q&A process, the system first retrieves relevant document fragments through vector similarity search, then provides the retrieval results as context to the large language model to generate accurate answers with citation sources.

Test results show that the system can accurately understand user questions, retrieve relevant documents, and generate high-quality answers. The system has good scalability and user experience, providing an effective solution for knowledge management and intelligent Q&A.

**Keywords:** Retrieval-Augmented Generation; Vector Database; Intelligent Q&A; Knowledge Base Management; Spring AI; Vue 3

---

## 目录

- [第一章 绪论](#第一章-绪论)
  - [1.1 研究背景与意义](#11-研究背景与意义)
  - [1.2 国内外研究现状](#12-国内外研究现状)
  - [1.3 研究内容与目标](#13-研究内容与目标)
  - [1.4 论文组织结构](#14-论文组织结构)
- [第二章 相关技术介绍](#第二章-相关技术介绍)
  - [2.1 RAG技术原理](#21-rag技术原理)
  - [2.2 向量数据库技术](#22-向量数据库技术)
  - [2.3 Spring Boot框架](#23-spring-boot框架)
  - [2.4 Vue 3框架](#24-vue-3框架)
  - [2.5 其他关键技术](#25-其他关键技术)
- [第三章 系统需求分析](#第三章-系统需求分析)
  - [3.1 功能需求分析](#31-功能需求分析)
  - [3.2 非功能需求分析](#32-非功能需求分析)
  - [3.3 用例分析](#33-用例分析)
- [第四章 系统设计](#第四章-系统设计)
  - [4.1 系统架构设计](#41-系统架构设计)
  - [4.2 数据库设计](#42-数据库设计)
  - [4.3 核心模块设计](#43-核心模块设计)
  - [4.4 UML类图设计](#44-uml类图设计)
- [第五章 系统实现](#第五章-系统实现)
  - [5.1 RAG问答模块实现](#51-rag问答模块实现)
  - [5.2 知识库管理模块实现](#52-知识库管理模块实现)
  - [5.3 文档向量化实现](#53-文档向量化实现)
  - [5.4 前端界面实现](#54-前端界面实现)
- [第六章 系统测试](#第六章-系统测试)
  - [6.1 测试环境](#61-测试环境)
  - [6.2 功能测试](#62-功能测试)
  - [6.3 性能测试](#63-性能测试)
- [第七章 总结与展望](#第七章-总结与展望)
  - [7.1 工作总结](#71-工作总结)
  - [7.2 未来展望](#72-未来展望)
- [参考文献](#参考文献)
- [致谢](#致谢)

---

## 第一章 绪论

### 1.1 研究背景与意义

#### 1.1.1 研究背景

在信息爆炸的时代，如何高效地管理和检索知识成为了一个重要课题。传统的知识管理系统主要依赖关键词搜索和数据库查询，这种方式存在以下问题：

1. **语义理解能力不足**：传统搜索引擎基于关键词匹配，无法理解用户问题的真实意图，导致检索结果不够精准。

2. **知识碎片化**：大量的文档、资料分散存储，用户需要在多个系统中查找信息，效率低下。

3. **缺乏智能交互**：传统系统只能返回文档列表，无法直接回答用户的问题，用户需要自行阅读大量文档才能找到答案。

近年来，大语言模型（Large Language Model，LLM）如GPT-4、Claude等的出现，为智能问答系统带来了新的可能。然而，纯粹依赖大语言模型也存在局限性：

1. **知识时效性问题**：模型的训练数据存在截止日期，无法获取最新信息。

2. **领域知识缺失**：通用模型缺乏特定领域的专业知识。

3. **幻觉问题**：模型可能生成看似合理但实际错误的内容。

检索增强生成（RAG）技术的出现有效解决了上述问题。RAG通过将向量检索与大语言模型相结合，既保留了LLM强大的语言理解和生成能力，又能够实时获取最新的、特定领域的知识，并提供可追溯的引用来源。

#### 1.1.2 研究意义

本研究的理论意义在于：

1. **探索RAG技术在知识管理中的应用**：研究如何将RAG技术与传统知识库系统结合，提升知识检索和问答的准确性。

2. **向量数据库的实践应用**：探索PgVector等向量数据库在实际项目中的应用方法和性能优化策略。

3. **多模态知识管理**：研究如何处理文本、图片等多种格式的文档，实现统一的知识表示和检索。

本研究的实际意义在于：

1. **提升知识检索效率**：通过语义检索替代传统关键词搜索，用户可以用自然语言提问，快速获得准确答案。

2. **降低知识管理成本**：系统自动完成文档解析、向量化等工作，减少人工整理知识的工作量。

3. **增强用户体验**：提供类似ChatGPT的对话式交互，用户无需学习复杂的查询语法，降低使用门槛。

4. **支持多场景应用**：系统可应用于企业知识库、在线教育、技术文档管理等多个场景。

### 1.2 国内外研究现状

#### 1.2.1 智能问答系统研究现状

智能问答系统的研究可以追溯到20世纪60年代的ELIZA系统。随着技术的发展，问答系统经历了以下几个阶段：

1. **基于规则的问答系统**：早期系统主要依赖人工编写的规则和模板，如ELIZA、ALICE等。这类系统灵活性差，难以处理复杂问题。

2. **基于信息检索的问答系统**：利用搜索引擎技术检索相关文档，然后从中提取答案。代表性系统包括IBM的Watson、微软的Bing等。

3. **基于深度学习的问答系统**：利用神经网络模型进行语义理解和答案生成。如BERT、GPT等预训练模型的出现大幅提升了问答系统的性能。

4. **基于RAG的问答系统**：结合检索和生成技术，既能利用外部知识库，又能生成流畅的自然语言回答。如Facebook的RAG模型、OpenAI的GPT-4 with Retrieval等。

#### 1.2.2 RAG技术研究现状

RAG技术最早由Facebook AI Research在2020年提出。其核心思想是将检索模块与生成模块结合：

1. **检索模块**：使用Dense Passage Retrieval（DPR）等技术，将文档和问题编码为向量，通过向量相似度检索相关文档。

2. **生成模块**：将检索到的文档作为上下文，输入到生成模型（如BART、T5）中，生成最终答案。

近年来，RAG技术得到了快速发展：

- **LangChain**：提供了完整的RAG开发框架，支持多种向量数据库和LLM。
- **LlamaIndex**：专注于文档索引和检索，提供了丰富的数据连接器。
- **Spring AI**：Spring生态系统中的AI框架，提供了RAG的Java实现。

国内方面，百度的文心一言、阿里的通义千问等也都集成了RAG功能，用于增强模型的知识获取能力。

#### 1.2.3 向量数据库研究现状

向量数据库是RAG系统的核心组件，用于存储和检索高维向量。主流的向量数据库包括：

1. **专用向量数据库**：
   - Pinecone：云原生向量数据库，提供高性能的相似度搜索。
   - Milvus：开源向量数据库，支持多种索引算法。
   - Weaviate：支持语义搜索和混合搜索的向量数据库。

2. **传统数据库的向量扩展**：
   - PgVector：PostgreSQL的向量扩展，支持向量存储和相似度搜索。
   - Elasticsearch：通过dense_vector类型支持向量检索。
   - Redis：通过RediSearch模块支持向量搜索。

本系统选择PgVector作为向量数据库，主要考虑到其与PostgreSQL的无缝集成，可以在同一数据库中管理结构化数据和向量数据，简化系统架构。

### 1.3 研究内容与目标

#### 1.3.1 研究内容

本研究的主要内容包括：

1. **RAG技术的应用研究**：
   - 研究RAG的工作原理和实现方法
   - 探索文档分割、向量化、检索等关键技术
   - 优化检索策略，提高答案准确性

2. **系统架构设计**：
   - 设计前后端分离的系统架构
   - 设计知识库管理、文档处理、问答交互等核心模块
   - 设计数据库模型和API接口

3. **关键功能实现**：
   - 实现多格式文档的解析和向量化
   - 实现基于向量相似度的文档检索
   - 实现流式RAG问答，提供引用来源
   - 实现知识库管理、内容管理等辅助功能

4. **系统测试与优化**：
   - 进行功能测试，验证系统的正确性
   - 进行性能测试，优化系统响应速度
   - 收集用户反馈，改进用户体验

#### 1.3.2 研究目标

本研究的目标是设计并实现一个功能完善、性能优良的智能知识库问答系统，具体目标包括：

1. **准确性目标**：系统能够准确理解用户问题，检索相关文档，生成正确的答案。

2. **实时性目标**：系统能够实时处理用户请求，响应时间控制在3秒以内。

3. **可扩展性目标**：系统支持多个知识库，可以方便地添加新的文档和知识。

4. **易用性目标**：提供友好的用户界面，支持对话式交互，降低使用门槛。

5. **可追溯性目标**：系统提供答案的引用来源，用户可以查看原始文档，增强可信度。

### 1.4 论文组织结构

本论文共分为七章，各章内容安排如下：

**第一章 绪论**：介绍研究背景、意义、国内外研究现状、研究内容与目标。

**第二章 相关技术介绍**：介绍RAG技术、向量数据库、Spring Boot、Vue 3等关键技术的原理和特点。

**第三章 系统需求分析**：分析系统的功能需求和非功能需求，给出用例图和用例描述。

**第四章 系统设计**：介绍系统的总体架构、数据库设计、核心模块设计，给出UML类图。

**第五章 系统实现**：详细介绍RAG问答、知识库管理、文档向量化等核心功能的实现方法和关键代码。

**第六章 系统测试**：介绍测试环境、测试方案和测试结果，验证系统的功能和性能。

**第七章 总结与展望**：总结研究工作，分析系统的优缺点，展望未来的改进方向。

---


## 第二章 相关技术介绍

### 2.1 RAG技术原理

#### 2.1.1 RAG技术概述

检索增强生成（Retrieval-Augmented Generation，RAG）是一种结合信息检索和文本生成的技术。其核心思想是在生成回答之前，先从外部知识库中检索相关信息，然后将检索到的信息作为上下文提供给生成模型，从而提高回答的准确性和可靠性。

RAG技术的工作流程如下：

1. **问题编码**：将用户的问题转换为向量表示。
2. **文档检索**：在向量数据库中检索与问题向量相似的文档片段。
3. **上下文构建**：将检索到的文档片段组织成上下文。
4. **答案生成**：将问题和上下文一起输入到大语言模型，生成最终答案。

#### 2.1.2 向量嵌入技术

向量嵌入（Embedding）是将文本转换为高维向量的技术。在RAG系统中，文档和问题都需要转换为向量，以便进行相似度计算。

常用的嵌入模型包括：

- **OpenAI Embedding**：OpenAI提供的text-embedding-ada-002模型，生成1536维向量，具有良好的语义表示能力。
- **BERT系列**：如sentence-transformers，支持多语言，可以本地部署。
- **BGE系列**：智源研究院开源的中文嵌入模型，在中文任务上表现优异。

本系统使用OpenAI Embedding模型，主要考虑其高质量的语义表示和与OpenAI LLM的良好兼容性。

#### 2.1.3 相似度检索算法

向量相似度检索是RAG的核心步骤。常用的相似度度量方法包括：

1. **余弦相似度（Cosine Similarity）**：
   ```
   similarity = (A · B) / (||A|| × ||B||)
   ```
   取值范围[-1, 1]，值越大表示越相似。

2. **欧氏距离（Euclidean Distance）**：
   ```
   distance = √(Σ(Ai - Bi)²)
   ```
   距离越小表示越相似。

3. **内积（Inner Product）**：
   ```
   similarity = A · B
   ```
   适用于归一化向量。

本系统默认使用余弦相似度，因为它对向量的长度不敏感，更适合语义相似度计算。

#### 2.1.4 文档分割策略

由于大语言模型的上下文窗口有限，需要将长文档分割成较小的片段（Chunk）。常用的分割策略包括：

1. **固定长度分割**：按字符数或token数分割，简单但可能破坏语义完整性。
2. **句子分割**：按句子边界分割，保持语义完整性。
3. **段落分割**：按段落分割，适合结构化文档。
4. **递归分割**：先按段落分割，如果片段过大再按句子分割，兼顾语义和长度。

本系统使用TokenTextSplitter，按token数量分割，同时设置重叠（overlap）以保持上下文连贯性。

### 2.2 向量数据库技术

#### 2.2.1 PgVector简介

PgVector是PostgreSQL的一个扩展，为PostgreSQL添加了向量数据类型和向量操作功能。其主要特点包括：

1. **无缝集成**：作为PostgreSQL扩展，可以在同一数据库中管理结构化数据和向量数据。
2. **标准SQL接口**：使用标准SQL语法进行向量操作，学习成本低。
3. **多种索引算法**：支持IVFFlat、HNSW等索引算法，提高检索效率。
4. **ACID保证**：继承PostgreSQL的事务特性，保证数据一致性。

#### 2.2.2 向量索引算法

为了提高大规模向量检索的效率，PgVector支持多种索引算法：

1. **IVFFlat（Inverted File with Flat compression）**：
   - 将向量空间划分为多个聚类
   - 检索时只搜索最近的几个聚类
   - 适合中等规模数据集（百万级）

2. **HNSW（Hierarchical Navigable Small World）**：
   - 构建多层图结构
   - 检索时从顶层开始逐层搜索
   - 检索速度快，适合大规模数据集（千万级）

本系统在数据量较小时使用暴力搜索，数据量增大后可创建HNSW索引以提高性能。

### 2.3 Spring Boot框架

#### 2.3.1 Spring Boot概述

Spring Boot是基于Spring框架的快速开发框架，提供了自动配置、起步依赖、内嵌服务器等特性，大幅简化了Spring应用的开发。

本系统使用Spring Boot 3.3.5版本，基于Java 21，主要利用了以下特性：

1. **自动配置**：自动配置数据源、JPA、Security等组件。
2. **起步依赖**：通过starter简化依赖管理。
3. **内嵌服务器**：内嵌Tomcat，无需单独部署。
4. **健康检查**：提供Actuator端点，监控应用状态。

#### 2.3.2 Spring AI框架

Spring AI是Spring生态系统中的AI框架，提供了与各种AI服务集成的能力。其主要组件包括：

1. **ChatModel**：统一的聊天模型接口，支持OpenAI、Azure OpenAI、Anthropic等。
2. **EmbeddingModel**：嵌入模型接口，用于生成文本向量。
3. **VectorStore**：向量存储接口，支持PgVector、Pinecone、Milvus等。
4. **DocumentReader**：文档读取器，支持PDF、Word、TXT等格式。
5. **Advisor**：顾问模式，用于增强对话能力，如RAG、记忆管理等。

本系统使用Spring AI 1.0.0-M4版本，实现了RAG问答的核心功能。

#### 2.3.3 MyBatis Plus

MyBatis Plus是MyBatis的增强工具，提供了CRUD接口、分页插件、代码生成器等功能，简化了数据库操作。

主要特性：

1. **BaseMapper**：提供了基础的CRUD方法，无需编写SQL。
2. **条件构造器**：提供了链式API构建查询条件。
3. **分页插件**：自动处理分页查询。
4. **逻辑删除**：支持逻辑删除，不物理删除数据。

### 2.4 Vue 3框架

#### 2.4.1 Vue 3概述

Vue 3是流行的前端框架，采用组合式API（Composition API），提供了更好的TypeScript支持和性能优化。

主要特性：

1. **响应式系统**：基于Proxy的响应式系统，性能更好。
2. **组合式API**：使用setup函数组织逻辑，代码更清晰。
3. **Teleport**：将组件渲染到DOM的其他位置。
4. **Suspense**：处理异步组件的加载状态。

#### 2.4.2 Vite构建工具

Vite是新一代前端构建工具，基于ES模块，提供了极快的开发服务器启动速度和热更新。

优势：

1. **快速冷启动**：利用浏览器原生ES模块，无需打包。
2. **即时热更新**：修改代码后立即生效。
3. **优化构建**：使用Rollup进行生产构建，输出优化的代码。

#### 2.4.3 UI组件库

本系统使用了两个UI组件库：

1. **Element Plus**：
   - 基于Vue 3的组件库
   - 提供了丰富的表单、表格、对话框等组件
   - 适合后台管理系统

2. **Naive UI**：
   - 轻量级Vue 3组件库
   - 提供了美观的UI设计
   - 支持TypeScript

### 2.5 其他关键技术

#### 2.5.1 MinIO对象存储

MinIO是开源的对象存储服务，兼容Amazon S3 API。本系统使用MinIO存储文档、图片、视频等文件。

主要特性：

1. **高性能**：支持高并发读写。
2. **分布式**：支持分布式部署，高可用。
3. **S3兼容**：兼容AWS S3 API，易于迁移。

#### 2.5.2 Redis缓存

Redis是高性能的内存数据库，本系统使用Redis作为缓存层，缓存热点数据，减少数据库查询。

应用场景：

1. **内容列表缓存**：缓存文章列表、推荐内容等。
2. **统计数据缓存**：缓存访问统计、活动统计等。
3. **标签缓存**：缓存内容标签、分类信息等。

#### 2.5.3 Apache Tika

Apache Tika是文档解析工具，支持PDF、Word、Excel、PPT等多种格式。

本系统使用Tika实现：

1. **文档内容提取**：提取文档的文本内容。
2. **元数据提取**：提取文档的标题、作者等元数据。
3. **格式检测**：自动检测文档格式。

---


## 第三章 系统需求分析

### 3.1 功能需求分析

#### 3.1.1 用户角色分析

本系统主要面向两类用户：

1. **普通用户**：
   - 浏览内容（文章、游戏、学习资源、视频）
   - 使用AI问答功能
   - 发表评论
   - 查看个人信息

2. **管理员**：
   - 管理内容（创建、编辑、删除）
   - 管理知识库（创建、上传文档、删除）
   - 管理评论（审核、推荐、删除）
   - 查看统计数据
   - 管理分类和标签

#### 3.1.2 核心功能需求

**1. AI智能问答功能**

- **简单对话**：用户可以与AI进行简单的对话交互，无需知识库支持。
- **RAG问答**：基于知识库的智能问答，系统检索相关文档后生成回答。
- **引用来源**：显示回答的引用来源，用户可以查看原始文档。
- **多模态对话**：支持上传图片进行问答。
- **对话历史**：保存对话记录，支持查看历史对话。
- **流式输出**：实时显示AI生成的回答，提升用户体验。

**2. 知识库管理功能**

- **知识库创建**：管理员可以创建多个知识库，每个知识库有独立的名称和描述。
- **文档上传**：支持上传PDF、Word、TXT等多种格式的文档。
- **文档解析**：自动解析文档内容，提取文本和图片。
- **向量化处理**：自动将文档内容转换为向量，存储到向量数据库。
- **文档管理**：查看、删除知识库中的文档。
- **知识库切换**：用户可以选择不同的知识库进行问答。

**3. 内容管理功能**

- **内容创建**：管理员可以创建文章、游戏、学习资源、视频等内容。
- **富文本编辑**：支持Markdown格式编辑内容。
- **封面上传**：支持上传内容封面图片。
- **视频管理**：支持上传视频文件或添加视频链接。
- **分类管理**：内容按分类组织（文章、游戏、学习、视频）。
- **标签管理**：为内容添加标签，方便检索。
- **推荐设置**：设置推荐内容，在首页展示。
- **内容同步**：内容自动同步到知识库，支持AI问答。

**4. 评论系统功能**

- **发表评论**：用户可以对内容发表评论。
- **头像上传**：支持上传评论者头像。
- **评论审核**：管理员可以审核评论。
- **评论推荐**：管理员可以设置推荐评论。
- **评论同步**：评论自动同步到知识库，丰富知识内容。

**5. 数据统计功能**

- **访问统计**：统计内容的浏览量。
- **活动热力图**：显示用户活动的时间分布。
- **AI助手报告**：AI自动生成数据分析报告。
- **快捷信息**：显示系统关键指标。

#### 3.1.3 辅助功能需求

**1. 用户认证与授权**

- **用户注册**：用户可以注册账号。
- **用户登录**：支持用户名密码登录。
- **JWT认证**：使用JWT实现无状态认证。
- **角色权限**：基于角色的访问控制（RBAC）。

**2. 文件存储管理**

- **对象存储**：使用MinIO存储文件。
- **多桶管理**：不同类型文件存储在不同的桶中。
- **临时访问**：生成临时访问URL，有效期7天。
- **文件校验**：使用MD5校验文件完整性。

**3. 缓存管理**

- **热点数据缓存**：缓存常用数据，减少数据库查询。
- **缓存过期**：设置合理的缓存过期时间。
- **缓存更新**：数据变更时自动清除相关缓存。

### 3.2 非功能需求分析

#### 3.2.1 性能需求

1. **响应时间**：
   - 普通页面加载时间 < 1秒
   - AI问答响应时间 < 3秒（首字响应 < 1秒）
   - 文档上传处理时间 < 10秒（取决于文档大小）

2. **并发能力**：
   - 支持100个并发用户同时访问
   - 支持10个并发AI问答请求

3. **数据库性能**：
   - 向量检索时间 < 500ms
   - 普通查询时间 < 100ms

#### 3.2.2 可靠性需求

1. **系统可用性**：系统可用性达到99%以上。
2. **数据备份**：定期备份数据库和文件存储。
3. **错误处理**：完善的异常处理机制，避免系统崩溃。
4. **日志记录**：记录系统运行日志，便于问题排查。

#### 3.2.3 安全性需求

1. **身份认证**：使用JWT进行身份认证。
2. **密码加密**：使用BCrypt加密用户密码。
3. **权限控制**：基于角色的访问控制。
4. **数据加密**：敏感数据加密存储。
5. **防护措施**：防止SQL注入、XSS攻击等。

#### 3.2.4 可扩展性需求

1. **模块化设计**：系统采用模块化设计，便于功能扩展。
2. **多知识库支持**：支持创建多个知识库。
3. **多模型支持**：支持配置不同的LLM模型。
4. **插件化**：支持添加新的文档解析器、向量数据库等。

#### 3.2.5 易用性需求

1. **友好界面**：提供简洁美观的用户界面。
2. **响应式设计**：支持PC和移动端访问。
3. **操作提示**：提供清晰的操作提示和错误信息。
4. **帮助文档**：提供系统使用文档。

### 3.3 用例分析

#### 3.3.1 用例图

```
                    +------------------+
                    |   普通用户        |
                    +------------------+
                            |
        +-------------------+-------------------+
        |                   |                   |
   [浏览内容]          [AI问答]           [发表评论]
        |                   |                   |
        |                   |                   |
                    +------------------+
                    |   管理员          |
                    +------------------+
                            |
        +-------------------+-------------------+
        |                   |                   |
   [管理内容]          [管理知识库]        [管理评论]
        |                   |                   |
   [查看统计]          [上传文档]         [审核评论]
```

#### 3.3.2 核心用例描述

**用例1：AI智能问答**

- **用例名称**：AI智能问答
- **参与者**：普通用户
- **前置条件**：用户已登录系统
- **主要流程**：
  1. 用户在问答页面输入问题
  2. 用户选择知识库（可选）
  3. 用户点击发送按钮
  4. 系统将问题转换为向量
  5. 系统在向量数据库中检索相关文档
  6. 系统将问题和检索结果发送给LLM
  7. 系统流式返回AI生成的回答
  8. 系统显示引用来源
- **后置条件**：对话记录保存到数据库
- **异常流程**：
  - 如果检索不到相关文档，系统提示"未找到相关信息"
  - 如果LLM调用失败，系统提示"服务暂时不可用"

**用例2：上传文档到知识库**

- **用例名称**：上传文档到知识库
- **参与者**：管理员
- **前置条件**：管理员已登录系统，知识库已创建
- **主要流程**：
  1. 管理员进入知识库管理页面
  2. 管理员选择目标知识库
  3. 管理员点击上传按钮，选择文档文件
  4. 系统上传文件到MinIO
  5. 系统使用Tika解析文档内容
  6. 系统使用TokenTextSplitter分割文档
  7. 系统使用Embedding模型生成向量
  8. 系统将向量存储到PgVector
  9. 系统更新文档状态为"已向量化"
- **后置条件**：文档可用于AI问答
- **异常流程**：
  - 如果文件格式不支持，系统提示"不支持的文件格式"
  - 如果向量化失败，系统提示"文档处理失败"

**用例3：创建内容**

- **用例名称**：创建内容
- **参与者**：管理员
- **前置条件**：管理员已登录系统
- **主要流程**：
  1. 管理员进入内容管理页面
  2. 管理员点击创建按钮
  3. 管理员填写标题、描述、内容等信息
  4. 管理员选择分类和标签
  5. 管理员上传封面图片（可选）
  6. 管理员点击保存按钮
  7. 系统保存内容到数据库
  8. 系统将内容同步到知识库
  9. 系统清除相关缓存
- **后置条件**：内容可被用户浏览，可用于AI问答
- **异常流程**：
  - 如果必填字段为空，系统提示"请填写完整信息"
  - 如果同步知识库失败，系统记录日志但不影响内容创建

**用例4：发表评论**

- **用例名称**：发表评论
- **参与者**：普通用户
- **前置条件**：用户已登录系统，内容已存在
- **主要流程**：
  1. 用户浏览内容详情页
  2. 用户在评论区输入评论内容
  3. 用户上传头像（可选）
  4. 用户点击提交按钮
  5. 系统保存评论到数据库
  6. 系统更新内容的评论数
  7. 系统将评论同步到知识库
  8. 系统清除相关缓存
- **后置条件**：评论显示在内容详情页，可用于AI问答
- **异常流程**：
  - 如果评论内容为空，系统提示"请输入评论内容"
  - 如果头像上传失败，使用默认头像

#### 3.3.3 系统边界

本系统的边界包括：

**系统内部**：
- AI问答功能
- 知识库管理
- 内容管理
- 评论系统
- 用户认证与授权
- 数据统计

**系统外部**：
- OpenAI API（LLM和Embedding服务）
- MinIO对象存储
- PostgreSQL数据库
- Redis缓存
- MySQL数据库

---

## 第四章 系统设计（续）

### 4.4 UML类图设计

#### 4.4.1 AI服务模块类图

```
┌─────────────────────────────────────┐
│      <<interface>>                  │
│      AIChatService                  │
├─────────────────────────────────────┤
│ + simpleChat(message): Flux         │
│ + ragChat(message, baseId): Flux   │
│ + ragChatWithReferences(): Response │
│ + multimodalChat(message): Flux    │
└─────────────────────────────────────┘
                 △
                 │ implements
                 │
┌─────────────────────────────────────┐
│      AIChatServiceImpl              │
├─────────────────────────────────────┤
│ - simpleChatModel: ChatModel        │
│ - longChatModel: ChatModel          │
│ - multimodalChatModel: ChatModel    │
│ - vectorStore: VectorStore          │
│ - embeddingModel: EmbeddingModel    │
├─────────────────────────────────────┤
│ + simpleChat(message): Flux         │
│ + ragChat(message, baseId): Flux   │
│ + ragChatWithReferences(): Response │
│ - extractReferences(docs): List     │
└─────────────────────────────────────┘
                 │
                 │ uses
                 ↓
┌─────────────────────────────────────┐
│      LLMService                     │
├─────────────────────────────────────┤
│ + getSimpleModel(): ChatModel       │
│ + getLongModel(): ChatModel         │
│ + getMultimodalModel(): ChatModel   │
└─────────────────────────────────────┘
```

#### 4.4.2 知识库管理模块类图

```
┌─────────────────────────────────────┐
│      KnowledgeBase                  │
├─────────────────────────────────────┤
│ - id: String                        │
│ - name: String                      │
│ - description: String               │
│ - createTime: LocalDateTime         │
│ - updateTime: LocalDateTime         │
└─────────────────────────────────────┘
                 │ 1
                 │
                 │ has
                 │
                 │ *
┌─────────────────────────────────────┐
│      DocumentEntity                 │
├─────────────────────────────────────┤
│ - id: Long                          │
│ - fileName: String                  │
│ - path: String                      │
│ - isEmbedding: Boolean              │
│ - baseId: String                    │
│ - resourceId: String                │
│ - createTime: LocalDateTime         │
├─────────────────────────────────────┤
│ + isEmbedded(): Boolean             │
└─────────────────────────────────────┘
                 │ 1
                 │
                 │ references
                 │
                 │ 1
┌─────────────────────────────────────┐
│      OriginFileResource             │
├─────────────────────────────────────┤
│ - id: String                        │
│ - fileName: String                  │
│ - path: String                      │
│ - bucketName: String                │
│ - objectName: String                │
│ - contentType: String               │
│ - size: Long                        │
│ - md5: String                       │
│ - images: List<String>              │
│ - createTime: LocalDateTime         │
└─────────────────────────────────────┘
```

#### 4.4.3 内容管理模块类图

```
┌─────────────────────────────────────┐
│      Content                        │
├─────────────────────────────────────┤
│ - id: String                        │
│ - title: String                     │
│ - description: String               │
│ - content: String                   │
│ - category: String                  │
│ - cover: String                     │
│ - viewCount: Integer                │
│ - commentCount: Integer             │
│ - isRecommend: Boolean              │
│ - contentType: String               │
│ - videoUrl: String                  │
│ - videoDuration: Integer            │
│ - createTime: LocalDateTime         │
│ - updateTime: LocalDateTime         │
├─────────────────────────────────────┤
│ + incrementViewCount(): void        │
│ + incrementCommentCount(): void     │
└─────────────────────────────────────┘
                 │ 1
                 │
                 │ has
                 │
                 │ *
┌─────────────────────────────────────┐
│      Comment                        │
├─────────────────────────────────────┤
│ - id: String                        │
│ - contentId: String                 │
│ - author: String                    │
│ - content: String                   │
│ - avatar: String                    │
│ - isRecommend: Boolean              │
│ - parentId: String                  │
│ - createTime: LocalDateTime         │
├─────────────────────────────────────┤
│ + isReply(): Boolean                │
└─────────────────────────────────────┘
```

#### 4.4.4 用户认证模块类图

```
┌─────────────────────────────────────┐
│      <<interface>>                  │
│      UserDetails                    │
└─────────────────────────────────────┘
                 △
                 │ implements
                 │
┌─────────────────────────────────────┐
│      SystemUser                     │
├─────────────────────────────────────┤
│ - id: Long                          │
│ - username: String                  │
│ - password: String                  │
│ - email: String                     │
│ - roles: List<SystemRole>           │
│ - permissions: List<SystemPermission>│
│ - createTime: LocalDateTime         │
│ - updateTime: LocalDateTime         │
├─────────────────────────────────────┤
│ + getAuthorities(): Collection      │
│ + isAccountNonExpired(): Boolean    │
│ + isAccountNonLocked(): Boolean     │
│ + isCredentialsNonExpired(): Boolean│
│ + isEnabled(): Boolean              │
└─────────────────────────────────────┘
                 │ *
                 │
                 │ has
                 │
                 │ *
┌─────────────────────────────────────┐
│      SystemRole                     │
├─────────────────────────────────────┤
│ - id: Long                          │
│ - name: String                      │
│ - description: String               │
│ - permissions: List<SystemPermission>│
└─────────────────────────────────────┘
                 │ *
                 │
                 │ has
                 │
                 │ *
┌─────────────────────────────────────┐
│      SystemPermission               │
├─────────────────────────────────────┤
│ - id: Long                          │
│ - name: String                      │
│ - description: String               │
└─────────────────────────────────────┘
```

#### 4.4.5 对话管理模块类图

```
┌─────────────────────────────────────┐
│      ChatConversation               │
├─────────────────────────────────────┤
│ - id: String                        │
│ - title: String                     │
│ - userId: Long                      │
│ - knowledgeBaseId: String           │
│ - createTime: LocalDateTime         │
│ - updateTime: LocalDateTime         │
└─────────────────────────────────────┘
                 │ 1
                 │
                 │ has
                 │
                 │ *
┌─────────────────────────────────────┐
│      ChatMessage                    │
├─────────────────────────────────────┤
│ - id: String                        │
│ - conversationId: String            │
│ - messageNo: Integer                │
│ - content: String                   │
│ - role: String                      │
│ - hasMedia: Boolean                 │
│ - resourceIds: List<String>         │
│ - isClean: Boolean                  │
│ - createTime: LocalDateTime         │
├─────────────────────────────────────┤
│ + isUserMessage(): Boolean          │
│ + isAssistantMessage(): Boolean     │
└─────────────────────────────────────┘
```

#### 4.4.6 服务层整体类图

```
┌──────────────────┐
│   Controller     │
│   Layer          │
└──────────────────┘
        ↓ calls
┌──────────────────────────────────────────────────────┐
│              Service Layer                           │
├──────────────────────────────────────────────────────┤
│  ┌────────────────┐  ┌────────────────┐            │
│  │ AIChatService  │  │ KnowledgeBase  │            │
│  │                │  │ Service        │            │
│  └────────────────┘  └────────────────┘            │
│  ┌────────────────┐  ┌────────────────┐            │
│  │ ContentService │  │ CommentService │            │
│  └────────────────┘  └────────────────┘            │
│  ┌────────────────┐  ┌────────────────┐            │
│  │ UserService    │  │ RedisService   │            │
│  └────────────────┘  └────────────────┘            │
└──────────────────────────────────────────────────────┘
        ↓ uses
┌──────────────────────────────────────────────────────┐
│              Repository Layer                        │
├──────────────────────────────────────────────────────┤
│  ┌────────────────┐  ┌────────────────┐            │
│  │ ContentMapper  │  │ CommentMapper  │            │
│  └────────────────┘  └────────────────┘            │
│  ┌────────────────┐  ┌────────────────┐            │
│  │ UserMapper     │  │ DocumentMapper │            │
│  └────────────────┘  └────────────────┘            │
└──────────────────────────────────────────────────────┘
        ↓ accesses
┌──────────────────────────────────────────────────────┐
│              Data Layer                              │
├──────────────────────────────────────────────────────┤
│  MySQL    PostgreSQL+PgVector    Redis    MinIO     │
└──────────────────────────────────────────────────────┘
```

### 4.5 接口设计

#### 4.5.1 AI问答接口

**1. 简单对话**

```
POST /ai/chat/simple
Content-Type: application/json

Request:
{
  "message": "你好，请介绍一下自己"
}

Response: (Server-Sent Events)
data: {"content": "你好", "finishReason": null}
data: {"content": "！", "finishReason": null}
data: {"content": "我是", "finishReason": null}
...
data: {"content": "", "finishReason": "STOP"}
```

**2. RAG问答（带引用）**

```
POST /ai/chat/simpleRAGWithReferences
Content-Type: application/json

Request:
{
  "conversationId": "uuid",
  "message": "什么是RAG技术？",
  "knowledgeBaseIds": ["base-id-1", "base-id-2"]
}

Response:
{
  "answer": Flux<Generation>,  // SSE流式返回
  "references": [
    {
      "type": "article",
      "id": "article-id",
      "title": "RAG技术介绍",
      "snippet": "RAG是检索增强生成技术..."
    },
    {
      "type": "comment",
      "id": "comment-id",
      "author": "张三",
      "articleTitle": "AI技术讨论",
      "snippet": "我认为RAG技术..."
    }
  ]
}
```

#### 4.5.2 知识库管理接口

**1. 创建知识库**

```
POST /knowledge/base
Content-Type: application/json

Request:
{
  "name": "技术文档库",
  "description": "存储技术相关文档"
}

Response:
{
  "code": 200,
  "message": "success",
  "data": {
    "id": "uuid",
    "name": "技术文档库",
    "description": "存储技术相关文档",
    "createTime": "2026-03-22T10:00:00"
  }
}
```

**2. 上传文档**

```
POST /file/upload
Content-Type: multipart/form-data

Request:
- file: (binary)
- knowledgeBaseId: "uuid"

Response:
{
  "code": 200,
  "message": "success",
  "data": {
    "resourceId": "uuid",
    "fileName": "document.pdf",
    "size": 1024000,
    "isEmbedding": true
  }
}
```

#### 4.5.3 内容管理接口

**1. 创建内容**

```
POST /content
Content-Type: application/json

Request:
{
  "title": "Vue 3入门教程",
  "description": "从零开始学习Vue 3",
  "content": "# Vue 3简介\n...",
  "category": "article",
  "tags": ["Vue", "前端"],
  "cover": "https://example.com/cover.jpg",
  "isRecommend": false
}

Response:
{
  "code": 200,
  "message": "success",
  "data": {
    "id": "uuid",
    "title": "Vue 3入门教程",
    ...
  }
}
```

**2. 获取内容列表**

```
GET /content/list?category=article&page=1&size=10

Response:
{
  "code": 200,
  "message": "success",
  "data": {
    "total": 100,
    "records": [
      {
        "id": "uuid",
        "title": "Vue 3入门教程",
        "description": "从零开始学习Vue 3",
        "category": "article",
        "cover": "https://example.com/cover.jpg",
        "viewCount": 1000,
        "commentCount": 50,
        "createTime": "2026-03-22T10:00:00"
      },
      ...
    ]
  }
}
```

#### 4.5.4 评论接口

**1. 发表评论**

```
POST /comment/create
Content-Type: multipart/form-data

Request:
- contentId: "uuid"
- author: "张三"
- content: "写得很好！"
- avatar: (binary, optional)

Response:
{
  "code": 200,
  "message": "success",
  "data": {
    "id": "uuid",
    "contentId": "uuid",
    "author": "张三",
    "content": "写得很好！",
    "avatar": "https://minio.example.com/avatars/xxx.jpg",
    "createTime": "2026-03-22T10:00:00"
  }
}
```

### 4.6 安全设计

#### 4.6.1 认证流程

```
用户登录
    ↓
[1] 提交用户名密码
    POST /auth/login
    ↓
[2] 验证用户信息
    UserDetailsService.loadUserByUsername()
    BCryptPasswordEncoder.matches()
    ↓
[3] 生成JWT Token
    JwtService.generateToken(user)
    - Header: {"alg": "HS256", "typ": "JWT"}
    - Payload: {"sub": username, "exp": timestamp, "roles": [...]}
    - Signature: HMACSHA256(header + payload, secret)
    ↓
[4] 返回Token
    {
      "token": "eyJhbGciOiJIUzI1NiIsInR5cCI6IkpXVCJ9...",
      "expiresIn": 604800
    }
    ↓
[5] 客户端存储Token
    localStorage.setItem('token', token)
    ↓
[6] 后续请求携带Token
    Authorization: Bearer <token>
```

#### 4.6.2 授权流程

```
请求受保护资源
    ↓
[1] JWT过滤器拦截
    JwtAuthenticationFilter.doFilterInternal()
    ↓
[2] 提取Token
    String token = request.getHeader("Authorization")
                         .substring(7)  // 去掉"Bearer "
    ↓
[3] 验证Token
    JwtService.validateToken(token)
    - 检查签名
    - 检查过期时间
    - 检查用户是否存在
    ↓
[4] 提取用户信息
    String username = JwtService.extractUsername(token)
    UserDetails user = UserDetailsService.loadUserByUsername(username)
    ↓
[5] 设置认证信息
    Authentication auth = new UsernamePasswordAuthenticationToken(
        user, null, user.getAuthorities()
    )
    SecurityContextHolder.getContext().setAuthentication(auth)
    ↓
[6] 权限检查
    @PreAuthorize("hasRole('ADMIN')")
    或
    @PreAuthorize("hasAuthority('content:create')")
    ↓
[7] 执行业务逻辑
```

#### 4.6.3 数据安全

1. **密码加密**：
```java
BCryptPasswordEncoder encoder = new BCryptPasswordEncoder();
String hashedPassword = encoder.encode(rawPassword);
```

2. **SQL注入防护**：
```java
// 使用MyBatis Plus的参数化查询
QueryWrapper<Content> wrapper = new QueryWrapper<>();
wrapper.eq("id", id);  // 自动参数化
```

3. **XSS防护**：
```java
// 前端使用DOMPurify清理HTML
// 后端对用户输入进行转义
String sanitized = HtmlUtils.htmlEscape(userInput);
```

4. **CORS配置**：
```java
@Configuration
public class WebMvcConfig implements WebMvcConfigurer {
    @Override
    public void addCorsMappings(CorsRegistry registry) {
        registry.addMapping("/**")
                .allowedOrigins("https://example.com")
                .allowedMethods("GET", "POST", "PUT", "DELETE")
                .allowedHeaders("*")
                .allowCredentials(true);
    }
}
```

---

