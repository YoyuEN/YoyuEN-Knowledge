# 个人知识库驱动的AI自我认知系统 — 毕业答辩PPT内容

---

## 第1页：封面

**个人知识库驱动的AI自我认知系统**

- 学 院：计算机科学与工程学院
- 专 业：软件工程
- 学生姓名：王泰乐
- 学 号：20221914
- 指导教师：周涛
- 答辩日期：2026年5月

---

## 第2页：目录

1. 项目背景与意义
2. 系统需求分析
3. 系统总体设计
4. 核心技术详解
5. 功能模块实现
6. 系统测试
7. 总结与展望

---

## 第3页：项目背景与意义

### 问题陈述
- 个人知识资产（笔记、文章、照片、经历等）分散在多个平台，难以统一管理
- 传统知识管理工具只是"数据仓库"，无法进行智能分析和深度洞察
- 用户难以回答"我是谁""我经历了什么""我如何成长"等自我认知核心问题

### 核心价值
- **从"存"到"懂"**：不只存储个人数据，更能基于数据帮助用户理解自己
- **数字化自我镜像**：构建结构化的"数字化自我"表征
- **第二大脑**：将分散的知识资产统一管理，对抗知识遗忘

### 研究切入点
- 将RAG（检索增强生成）技术与自我认知理论相结合
- 填补"通用AI缺乏个人上下文"的技术空白

---

## 第4页：国内外研究现状

### 现有方案的三个断层

| 断层 | 现状 | 问题 |
|------|------|------|
| 技术层与认知层 | RAG技术成熟但多用于企业场景 | 未针对"个人自我认知"场景 |
| 数据层与洞察层 | 工具可存储多格式内容 | 不会分析背后模式 |
| 多模态数据层 | 个人资产包括文字/图片/视频 | 缺少统一管理与关联 |

### 本系统的差异化

```
传统工具：存 → 找
智能工具(Mem.ai等)：存 → 找 → 简单摘要
本系统：存 → 找 → 理解 → 洞察 → 帮助自我认知
```

---

## 第5页：系统需求分析 — 功能模块总览

### 系统功能模块图（树形结构）

```
个人知识库驱动的AI自我认知系统
├── 用户内容模块（前台）
│   ├── 首页浏览（热力图、推荐文章/视频）
│   ├── 内容浏览（分类筛选、详情阅读、浏览量统计）
│   ├── 札记动态（时光手札）
│   ├── 照片墙（个人经历照片展示）
│   ├── 评论互动（发表评论、自定义头像）
│   └── AI RAG问答（多知识库切换、流式输出、引用来源）
└── 后台管理模块（需认证）
    ├── 仪表盘（AI系统报告SSE流式推送、数据统计）
    ├── 内容管理（文章/视频发布、Markdown编辑、标签关联）
    ├── 分类管理（新增/修改/删除、内容批量迁移）
    ├── 标签管理
    ├── 评论管理（推荐评论、批量删除）
    ├── 知识库管理（创建/删除/文档上传/向量化状态）
    └── 照片/札记管理
```

---

## 第6页：核心业务流程 — 用户登录与RAG问答

### 用户登录认证流程
```
输入凭证 → 参数校验 → 查询用户 → BCrypt验证密码 → 生成JWT → 返回Token
                                     ↓ 失败
                                  返回401
```

### RAG问答核心流程（系统最核心业务）
```
用户提问 → Embedding问题向量化 → PgVector相似度检索
    → 获取Top-K知识片段 → 构建增强Prompt（问题+检索上下文）
    → 大语言模型生成回答 → SSE流式逐字输出 → 展示引用来源
```

**技术要点**：
- 向量检索：PgVector HNSW索引，余弦相似度
- 上下文构建：检索结果拼接至Prompt模板
- 流式输出：Spring WebFlux + SSE协议
- 可追溯性：回答附带知识来源引用

---

## 第7页：核心业务流程 — 文档向量化与内容发布

### 文档上传与向量化全链路
```
选择文档(PDF/Word/TXT/MD) → 格式校验 → 上传MinIO → Apache Tika解析纯文本
    → TokenTextSplitter切分(800 token/片段, 200 token重叠)
    → OpenAI Embedding生成1536维向量 → 批量写入PgVector → 更新状态为"已向量化"
```

### "写博客即构建知识库"的设计理念
```
管理员发布内容 → 持久化到content表
    → 异步转为Markdown → 同步到知识库 → 自动向量化
    → AI问答即可检索引用 → 知识库自动增长
```

---

## 第8页：五层系统架构

### 整体架构（自上而下单向依赖）

```
┌─────────────────────────────────────────┐
│  表现层 (Presentation)                   │
│  Vue 3 + Element Plus + Pinia + Router   │
├─────────────────────────────────────────┤
│  应用层 (Application)                     │
│  Nginx反向代理 + API Gateway             │
├─────────────────────────────────────────┤
│  逻辑层 (Business Logic)                  │
│  Spring Boot 3.3.5  Controller/Service   │
├─────────────────────────────────────────┤
│  数据层 (Data Access)                     │
│  MyBatis Plus / Spring AI / JPA          │
├─────────────────────────────────────────┤
│  持久层 (Persistence)                     │
│  PostgreSQL+PgVector / Redis / MinIO     │
└─────────────────────────────────────────┘ 
```

**分层原则**：每层只依赖下一层，层间通过定义良好的接口通信

---

## 第9页：Docker容器化部署架构

### 七容器编排（Docker Compose）

```
                   ┌──────────────┐
                   │   Nginx:443  │ ← 唯一对外端口
                   └──────┬───────┘
                          │
          ┌───────────────┼───────────────┐
          ▼               ▼               ▼
   ┌────────────┐  ┌────────────┐  ┌────────────┐
   │  frontend  │  │  backend   │  │   minio    │
   │  Nginx:80  │  │ :12138/api │  │  :9000/9001│
   └────────────┘  └──┬──┬──┬──┘  └────────────┘
                      │  │  │
          ┌───────────┘  │  └───────────┐
          ▼              ▼              ▼
   ┌────────────┐ ┌────────────┐ ┌────────────┐
   │ postgres   │ │   redis    │ │  LLM API   │
   │ +PgVector  │ │   :6379    │ │  (外部)    │
   │   :5432    │ └────────────┘ └────────────┘
   └────────────┘
```

### 容器化优势
- 环境一致性：开发/生产环境相同Docker镜像
- 一键部署：`docker compose up -d`
- 数据持久化：PostgreSQL/Redis/MinIO挂载Volume
- 安全隔离：仅Nginx暴露公网，其余容器仅在内部网络通信

---

## 第10页：数据库设计

### 核心数据表（16张表，4大模块）

| 模块 | 核心表 | 说明 |
|------|--------|------|
| 用户权限 | system_user, system_role, system_permission, system_user_role, system_role_permission | RBAC权限模型，BCrypt密码加密 |
| 知识库 | knowledge_base, document_entity, origin_file_source | 知识库元数据 + 文档向量化状态 + MinIO文件元数据 |
| 内容管理 | content, comment, murmur, photo, content_tag, content_tag_relation | 博客文章/视频 + 评论嵌套回复 + 碎碎念短动态 + 照片墙 |
| AI对话 | chat_conversation, chat_message | 会话-消息二级聚合，多轮对话上下文管理 |

### 设计原则
- 三范式规范化，消除数据冗余
- 审计字段统一（create_time, update_time, deleted, creator, updater）
- UUID主键 + 逻辑删除（MyBatis Plus @TableLogic）
- B-Tree索引 + PgVector HNSW索引

---

## 第11页：核心技术栈详解

| 层次 | 技术 | 版本 | 作用 |
|------|------|------|------|
| 后端框架 | Spring Boot | 3.3.5 | IoC容器、自动配置、REST API |
| AI框架 | Spring AI | 1.0.0-M4 | 统一AI模型抽象、VectorStore接口 |
| LLM | 通义千问 qwen-max | - | 大语言模型，RAG答案生成 |
| Embedding | text-embedding-ada-002 | - | 文本向量化（1536维） |
| 图像生成 | 豆包 Seedream 4.5 | - | 图片动漫化处理 |
| ORM | MyBatis Plus | 3.5.10 | 条件构造器、分页插件、逻辑删除 |
| 安全 | Spring Security + JWT | 6.x / 0.12.5 | 认证授权、BCrypt加密、无状态Token |
| 文档解析 | Apache Tika | 3.0.0 | 100+格式文档统一解析 |
| 关系数据库 | PostgreSQL | 16 | 业务数据 + 向量数据（PgVector扩展） |
| 向量扩展 | PgVector | 0.5.1 | HNSW索引，余弦相似度检索 |
| 缓存 | Redis | 7.2 | 内容缓存、会话管理、统计数据 |
| 对象存储 | MinIO | latest | 文件/图片/视频 S3兼容存储 |
| 前端框架 | Vue 3 | 3.4 | 组合式API + Proxy响应式 |
| 构建工具 | Vite | - | 快速HMR、ES模块打包 |
| UI组件库 | Element Plus | - | 管理后台组件 |
| 容器化 | Docker Compose | - | 七容器编排部署 |

---

## 第12页：AI服务模块详细设计（核心模块）

### 类图关键设计

```
<<interface>>          <<interface>>
AIChatService          KnowledgeBaseService
├ simpleChat()         ├ KnowledgeList()
├ unifyChat()          └ ...
├ simpleRAGChat()
└ multimodalChat()
        ↑ 实现
AIChatServiceImpl
├ simpleChat → ChatClient (流式对话)
├ simpleRAG → QuestionAnswerAdvisor (RAG问答)
└ simpleRAGWithReferences → 带引用来源
```

### RAG问答调用链（顺序图）

```
用户 → Vue前端 → AIChatController → AIChatService
    → EmbeddingModel (问题向量化)
    → PgVectorStore.similaritySearch() (检索Top-K)
    → Prompt构建 (问题 + 检索上下文)
    → LLM调用 (流式生成)
    → SSE输出 + 引用来源
    → chat_message表存储
```

### 关键技术细节
- **向量检索**：HNSW索引，余弦相似度，默认检索Top-5相关片段
- **Prompt模板增强**：`"根据以下知识回答问题：{context}\n\n问题：{question}"`
- **流式输出**：`Flux<ChatResponse>` + `MediaType.TEXT_EVENT_STREAM_VALUE`
- **引用来源**：检索结果附带文档名和片段内容，回答末尾展示

---

## 第13页：文档向量化Pipeline实现

### 处理流程

```
┌────────────┐   ┌──────────────┐   ┌───────────────┐
│ 文件上传    │ → │ Apache Tika  │ → │ TokenTextSplitter │
│ (PDF/Word/ │   │ 文档解析     │   │ 文本切分        │
│  TXT/MD)   │   │ → 纯文本     │   │ 800token/片     │
└────────────┘   └──────────────┘   └───────┬───────┘
                                            │
              ┌─────────────────────────────┘
              ▼
┌─────────────────────┐   ┌─────────────────────┐
│ OpenAI Embedding    │ → │ PgVector批量写入     │
│ → 1536维向量        │   │ + 元数据存储         │
└─────────────────────┘   └─────────────────────┘
```

### 技术要点
- **文件格式支持**：PDF（PDFBox）、Word/Excel/PPT（Apache POI）、TXT、Markdown
- **智能分片**：每片段800 token，保留200 token重叠防止语义截断
- **异步处理**：Spring `@Async` 线程池，上传后立即返回，后台异步向量化
- **状态追踪**：`document_entity.is_embedding` 字段标识处理状态
- **一致性保证**：文档删除时同步清理向量数据，防止"脏检索"

---

## 第14页：前端界面实现

### Vue 3技术架构

```
src/
├── api/          # Axios请求封装（12个模块）
├── components/   # 11个可复用组件
├── views/        # 19个页面视图
├── router/       # 路由配置 + 导航守卫
├── stores/       # Pinia状态管理
├── composables/  # 组合式函数
├── directives/   # 自定义指令（长按等）
└── utils/        # 工具函数（认证、请求拦截）
```

### 两大核心界面

**AI对话界面（Home.vue）**
- 左侧：知识库选择面板 + 对话历史列表
- 右侧：消息气泡展示区 + Markdown渲染
- 底部：文本输入 + 图片上传 + 发送
- 特性：SSE流式逐字渲染、引用卡片可点击跳转、Markdown代码高亮

**知识库管理界面（KnowledgeList.vue）**
- 左侧：知识库列表（树形菜单）
- 右侧：文档表格（文件名/类型/时间/向量化状态标签）
- 操作：新建知识库、上传文档（多格式）、下载/删除

### 前端特色技术
- **流式渲染**：SSE EventSource逐字追加到消息数组
- **响应式布局**：Element Plus + CSS Flex/Grid自适应
- **状态管理**：Pinia stores管理用户认证状态
- **路由守卫**：`router.beforeEach` 拦截未认证访问后台管理

---

## 第15页：安全体系设计

### 多层次安全防护

| 防护层 | 技术手段 |
|--------|----------|
| 传输安全 | Nginx HTTPS/TLS 终端 |
| 认证层 | JWT无状态令牌 + BCrypt密码加密（自动加盐） |
| 授权层 | Spring Security RBAC（用户-角色-权限 5表模型） |
| 输入防护 | MyBatis Plus参数化查询防SQL注入 + HTML转义防XSS |
| 密钥管理 | API密钥通过环境变量注入，不硬编码 |
| 网络隔离 | 仅Nginx暴露公网，数据库/缓存仅在Docker内网 |
| CORS | 限制跨域请求来源 |

### 权限设计策略
- **前台开放**：访客无需登录即可浏览内容、使用AI问答
- **后台授权**：管理操作（内容发布、知识库管理等）需JWT认证
- Token过期自动跳转登录页，路由守卫前端拦截 + 后端Security双重校验

---

## 第16页：系统测试

### 测试策略
- **方法**：黑盒测试（等价类划分 + 边界值分析 + 场景法）
- **覆盖**：4大核心模块，26条测试用例

### 核心模块测试概要

| 模块 | 测试用例数 | 关键验证点 |
|------|-----------|-----------|
| AI问答 | 6 | 简单对话、知识库RAG、流式输出、上下文保持、空输入拦截、未知内容处理 |
| 知识库管理 | 7 | 创建/删除知识库、多格式文档上传、向量化完成、非法格式/超大文件拦截 |
| 内容管理 | 7 | 发布文章/视频、Markdown渲染、标签关联、知识库自动同步、必填字段校验 |
| 后台登录 | 6 | 正确/错误凭证、空输入校验、越权访问拦截、过期Token处理 |

### 测试结论
所有26条测试用例均通过，功能实现符合需求规格说明书要求。测试

### 测试环境
- 云服务器：2核CPU / 2GB内存 / 50GB SSD
- OS：Ubuntu Server 24.04 LTS
- JDK：OpenJDK 21 / PostgreSQL 16 + PgVector 0.5.1 / Redis 7.2

---

## 第17页：总结与展望

### 完成的工作

| 方面 | 成果 |
|------|------|
| **系统架构** | 前后端分离 + 五层架构 + Docker七容器编排部署 |
| **RAG问答** | 问题向量化→检索→增强Prompt→LLM流式生成→引用来源全链路实现 |
| **知识库管理** | 多格式文档解析(Tika)→智能分片(TokenTextSplitter)→向量化(Embedding)→存储(PgVector) |
| **内容管理** | "写博客即构建知识库"，发布内容自动同步知识库并向量化 |
| **安全体系** | Spring Security + JWT + BCrypt + RBAC + 防SQL注入/XSS |

### 技术创新点
1. **RAG + 自我认知**：首次将RAG技术系统性地应用于个人自我认知场景
2. **写博客即构建知识库**：内容发布与知识库向量化自动联动
3. **带引用的可信回答**：AI回答附带知识来源，增强可信度
4. **多模态统一管理**：文本、图片、视频统一存储与向量化

### 未来改进方向
- **知识图谱融合**：引入个人知识图谱技术，将碎片化文档自动抽取为实体-关系网络，增强知识之间的语义关联与推理能力
- **本地模型部署**：接入 Ollama / vLLM 等本地推理引擎，支持 Qwen、DeepSeek 等开源模型私有化部署，降低 API 调用成本并提升数据隐私
- **多模态深度理解**：利用视觉大模型对图片内容进行语义提取与向量化，使照片墙中的图片也能参与 RAG 检索
- **移动端适配**：开发微信小程序或 React Native 移动端，支持随时随地的知识记录与 AI 对话
- **智能记忆与主动推送**：基于用户行为分析，实现"知识回顾"主动推送——系统定期提醒用户回顾历史记录，强化自我认知闭环

### 致谢

时光荏苒，四年大学时光转眼就要画上句号。回头看看这一路，有熬夜刷题的疲惫，也有AC的兴奋；有被bug卡住的烦躁，也有跑通那一刻的畅快。虽然磕磕绊绊，但庆幸遇见了许多良师益友，让这段青春变得充实又珍贵。

首先要感谢一位带我入门的学长。从大一懵懵懂懂跟着他刷算法题，到后来一起备战竞赛、讨论技术方向，是他让我看清了"程序员的成长路径"到底该怎么走。即使后来他去了外校访学、联系渐渐少了，但他那套"多刷题、多动手、多思考"的学习方法，到现在还影响着我。说实话，这个毕设项目能从头到尾撸出来，跟他当年帮我打下的底子有很大关系。

衷心感谢我的毕设指导老师周涛老师。说实话我的选题方向跟老师日常的研究领域不太一样，但老师从来没有因为这个敷衍我。从选题申报、开题报告到论文初稿，每一轮老师都认真看了，提出的修改意见也都很具体——哪个图标注不清晰、哪段逻辑没说透、哪里应该加个对比实验，处处都点到了。正是这些建议，让我把一个起初有点"飘"的想法，一点点打磨成了现在能拿出来的作品。

还要感谢一下那个一路没放弃的自己。从去年底开始构思这个项目，到后来搭后端、写前端、调模型、整部署，中间无数次想"算了就这样吧"，但最后还是咬着牙把该做的都做完了。辛苦了。

最后，感谢各位评审老师在百忙之中抽时间看我的论文和答辩，你们提出的每一条意见对我来说都是宝贵的成长机会。由于水平有限，论文和系统肯定还有不足的地方，恳请老师们批评指正。

---

## 附录：答辩建议

### PPT制作建议
- 总页数控制在18-22页
- 重点展示第11页（技术栈）、第8页（系统架构）、第6-7页（核心流程）
- 每个流程配一张流程图（可参考论文中的图3.1-3.5、3.8-3.9）
- 系统截图放4-6张：AI对话界面、知识库管理界面、文章发布界面、仪表盘

### 答辩时间分配建议（总计15分钟）
- 项目背景 + 需求分析：3分钟
- 系统设计 + 核心技术：5分钟
- 功能实现展示：4分钟
- 测试 + 总结展望：3分钟

### 可能被问到的问题
1. RAG技术在本系统中的具体实现流程是什么样的？
2. 为什么选择PgVector而不是Milvus/Weaviate等专业向量数据库？
3. 文档分割时为什么选择800 token、200 token重叠？有哪些考量？
4. 如何处理大模型"幻觉"问题？引用机制如何保证回答准确性？
5. 系统的安全性是如何保证的？
6. Docker Compose的七容器是如何划分的？部署架构是怎样的？
7. 与传统个人知识管理工具（Notion/Obsidian）相比，本系统的核心优势是什么？
