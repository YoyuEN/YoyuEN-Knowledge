# 视频上传功能测试指南

## 问题排查

### 404 错误解决方案

如果遇到 `404 (Not Found)` 错误，请按以下步骤检查：

#### 1. 确认后端服务已启动

后端配置：
- 端口：`12138`
- 上下文路径：`/api`
- 完整地址：`http://localhost:12138/api`

**启动后端：**
```bash
cd Backend
mvn spring-boot:run
# 或者在 IDE 中运行 BackendApplication.java
```

**验证后端是否运行：**
```bash
curl http://localhost:12138/api/content/stats
```

如果返回 JSON 数据，说明后端正常运行。

#### 2. 检查前端代理配置

前端需要配置代理将 `/api` 请求转发到后端。

**检查 `vite.config.js` 或 `vite.config.ts`：**
```javascript
export default defineConfig({
  server: {
    port: 5173,
    proxy: {
      '/api': {
        target: 'http://localhost:12138',
        changeOrigin: true,
        // rewrite: (path) => path.replace(/^\/api/, '')  // 不需要重写，因为后端已有 /api 前缀
      }
    }
  }
})
```

#### 3. 重启前端开发服务器

修改配置后需要重启：
```bash
cd Fronted
npm run dev
```

#### 4. 检查数据库字段

确保数据库已添加视频相关字段：
```sql
-- 检查字段是否存在
SELECT column_name, data_type
FROM information_schema.columns
WHERE table_name = 'content'
AND column_name IN ('content_type', 'video_type', 'video_url', 'video_duration');

-- 如果字段不存在，执行以下 SQL
ALTER TABLE content ADD COLUMN content_type VARCHAR(20) DEFAULT 'article';
ALTER TABLE content ADD COLUMN video_type VARCHAR(10);
ALTER TABLE content ADD COLUMN video_url VARCHAR(500);
ALTER TABLE content ADD COLUMN video_duration INT DEFAULT 0;
```

## 测试步骤

### 1. 测试视频文件上传

1. 访问后台管理：`http://localhost:5173/admin/articles`
2. 点击"新增文章"按钮
3. 选择"视频内容"类型
4. 选择"上传视频文件"
5. 拖拽或点击上传一个测试视频（建议先用小文件测试，如 10MB 以内）
6. 填写标题、分类、简介
7. 点击保存

**预期结果：**
- 上传进度条显示
- 上传成功后显示视频预览
- 保存成功提示

### 2. 测试视频链接

1. 点击"新增文章"
2. 选择"视频内容"类型
3. 选择"视频链接"
4. 输入测试链接：
   - B站：`https://www.bilibili.com/video/BV1xx411c7mD`
   - YouTube：`https://www.youtube.com/watch?v=dQw4w9WgXcQ`
5. 填写其他信息并保存

**预期结果：**
- 自动识别视频平台
- 显示视频预览（iframe）
- 保存成功

### 3. 测试前台展示

1. 访问内容页面：`http://localhost:5173/content`
2. 找到刚创建的视频内容
3. 点击进入详情页

**预期结果：**
- 视频播放器正常显示
- 可以播放视频
- 控制条功能正常（播放/暂停、音量、全屏等）

## 常见问题

### Q1: 上传时提示"视频大小不能超过 500MB"
**A:** 这是正常的限制。如需上传更大的视频，需要：
1. 修改前端 `VideoUploader.vue` 中的 `maxSize` 属性
2. 修改后端 `application.properties` 中的 `spring.servlet.multipart.max-file-size`

### Q2: 上传成功但视频无法播放
**A:** 可能的原因：
1. 视频格式不被浏览器支持（建议使用 MP4 格式）
2. 视频编码不兼容（建议使用 H.264 编码）
3. MinIO 存储服务未正常运行

### Q3: B站视频链接无法播放
**A:** 检查链接格式是否正确：
- 正确：`https://www.bilibili.com/video/BV1xx411c7mD`
- 错误：`https://b23.tv/xxxxx`（短链接需要先转换）

### Q4: 视频上传很慢
**A:** 优化建议：
1. 使用有线网络而非 WiFi
2. 压缩视频文件
3. 考虑使用 CDN 加速
4. 实现分片上传（需要额外开发）

## API 接口文档

### 上传视频文件
```
POST /api/content/upload-video
Content-Type: multipart/form-data

参数：
- file: 视频文件

返回：
{
  "code": 200,
  "data": {
    "url": "视频访问URL",
    "path": "存储路径",
    "cover": "封面URL",
    "coverPath": "封面路径",
    "duration": 0
  }
}
```

### 提取视频封面
```
POST /api/content/extract-video-cover
Content-Type: application/json

参数：
{
  "videoUrl": "视频URL"
}

返回：
{
  "code": 200,
  "data": {
    "cover": "封面URL"
  }
}
```

### 创建内容
```
POST /api/content/create
Content-Type: application/json

参数：
{
  "title": "标题",
  "description": "简介",
  "category": "分类",
  "cover": "封面URL",
  "contentType": "video",
  "videoType": "file",
  "videoUrl": "视频URL",
  "videoDuration": 120,
  "tags": ["标签1", "标签2"]
}
```

## 配置检查清单

- [ ] 后端服务已启动（端口 12138）
- [ ] 前端代理配置正确
- [ ] 数据库字段已添加
- [ ] MinIO 存储服务正常
- [ ] Redis 服务正常
- [ ] 文件上传大小限制已调整为 500MB
- [ ] 前端开发服务器已重启

## 联系支持

如果以上步骤都无法解决问题，请提供：
1. 浏览器控制台完整错误信息
2. 后端日志输出
3. 网络请求详情（Network 标签）
