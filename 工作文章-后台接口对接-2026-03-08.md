# 后台接口对接与管理功能实现说明（2026-03-08）

## 一、目标与范围
本次工作目标：
1. 将 `Fronted/src/views/admin` 后台页面改为真实后端接口驱动。
2. 实现文章/分类/标签/评论/图片相关管理能力。
3. 保证移动端可用、按钮均有反馈，避免“点击无反应”。
4. 界面风格去 AI 化，采用简洁中性灰度风格。
5. 实现关键业务约束：删除文章前必须先删除对应评论。

## 二、核心改动总览

### 1) 后端改动（Backend）
- `controller`
  - `ContentController.java`
  - `CommentController.java`
- `service`
  - `ContentService.java`
  - `CommentService.java`
  - `impl/ContentServiceImpl.java`
  - `impl/CommentServiceImpl.java`
- `vo`
  - `ContentVO.java`（新增 `tags`、`categoryName`）
  - `ContentTagVO.java`（新增）

### 2) 前端改动（Fronted）
- `api`
  - `src/api/content/content.js`
  - `src/api/comment/comment.js`
- `admin 页面`
  - `src/components/AdminLayout.vue`
  - `src/views/admin/admin-theme.css`
  - `src/views/admin/Dashboard.vue`
  - `src/views/admin/ArticleList.vue`
  - `src/views/admin/CategoryList.vue`
  - `src/views/admin/TagList.vue`
  - `src/views/admin/CommentList.vue`（新增）
  - `src/views/admin/PhotoList.vue`（新增）
  - `src/views/admin/Statistics.vue`
- `路由与请求`
  - `src/router/router.js`
  - `src/utils/request.js`

## 三、后端接口实现细节

### 1) 文章管理相关
新增/完善接口：
- `GET /api/content/all`：后台文章列表
- `POST /api/content/create`：创建文章
- `POST /api/content/update`：更新文章（支持 Markdown 正文、分类、标签、封面路径）
- `POST /api/content/remove`：删除文章（带评论前置校验）
- `POST /api/content/recommend`：推荐状态切换
- `POST /api/content/upload-cover`：封面上传

业务规则：
- 删除文章时，先检查评论数量：`commentService.countByContentId(contentId)`。
- 若评论 > 0，返回业务错误：`请先删除评论后再删除文章`。

### 2) 分类管理相关
新增接口：
- `GET /api/content/categories`：分类列表（含文章计数）
- `POST /api/content/category/create`
- `POST /api/content/category/update`
- `POST /api/content/category/remove`

业务规则：
- 删除分类前检查该分类下文章数，存在文章则禁止删除。

### 3) 标签管理相关
新增接口：
- `GET /api/content/tags`：标签列表（含使用计数）
- `POST /api/content/tag/create`
- `POST /api/content/tag/update`
- `POST /api/content/tag/remove`

业务规则：
- 删除标签前检查是否被文章使用，若有引用则禁止删除。

### 4) 评论管理相关
新增/完善接口：
- `GET /api/comment/all`：后台评论列表
- `POST /api/comment/remove`：单条删除
- `POST /api/comment/remove/by-content`：按文章批量删除
- `GET /api/comment/count`：支持按 `contentId` 统计
- `POST /api/comment/recommend`：推荐切换

并补齐评论计数回退：
- 删除评论后调用 `contentService.decrementCommentCount(contentId, delta)`。

## 四、前端功能实现细节

### 1) 文章管理（ArticleList.vue）
已实现：
- 列表加载与筛选（关键字 + 分类）
- 新增/编辑弹窗
- Markdown 编辑与预览切换
- 封面上传
- 标签多选
- 推荐状态切换
- 删除逻辑（自动处理“先删评论再删文章”）

关键交互：
- 若后端返回“请先删除评论”，前端弹框确认后调用：
  1. `/api/comment/remove/by-content`
  2. `/api/content/remove`

### 2) 分类管理（CategoryList.vue）
已实现：
- 分类列表
- 新增分类
- 编辑分类（支持标识变更）
- 删除分类（后端校验是否可删）

### 3) 标签管理（TagList.vue）
已实现：
- 标签列表
- 新增标签
- 标签重命名
- 删除标签（后端校验是否被引用）

### 4) 评论管理（CommentList.vue）
已实现：
- 评论列表查询
- 推荐状态切换
- 删除评论

### 5) 图片管理（PhotoList.vue）
已实现：
- 图片上传
- 图片列表展示
- 删除图片

### 6) 仪表盘与统计
- 仪表盘：`content stats + category + tag` 数据展示
- 统计页：近 30 天发布数据列表

## 五、移动端适配与风格优化

### 1) 移动端适配
通过 `admin-theme.css` 增加断点：
- `992px`：收缩头部信息，简化布局
- `768px`：工具栏输入控件全宽、卡片单列、主内容紧凑间距

### 2) 视觉风格（去 AI 化）
- 去除彩色渐变背景，采用灰/白/深灰中性色。
- 主按钮使用深灰色，普通按钮浅灰背景。
- 卡片、表格、输入框、弹窗统一边框与色阶，风格简洁稳定。

## 六、关键代码片段（节选）

### 1) 文章删除前评论校验（后端）
文件：`Backend/src/main/java/com/yoyuen/backend/controller/ContentController.java`
```java
int commentCount = commentService.countByContentId(contentVO.getId());
if (commentCount > 0) {
    return ResultUtils.error(CoreCode.OPERATION_ERROR,
        "该文章下仍有评论（" + commentCount + " 条），请先删除评论后再删除文章");
}
```

### 2) 按文章批量删除评论（后端）
文件：`Backend/src/main/java/com/yoyuen/backend/controller/CommentController.java`
```java
@PostMapping("/remove/by-content")
public BaseResponse<Integer> removeByContent(@RequestBody CommentVO commentVO) {
    int removed = commentService.removeByContentId(commentVO.getContentId());
    if (removed > 0) {
        contentService.decrementCommentCount(commentVO.getContentId(), removed);
    }
    return ResultUtils.success(removed);
}
```

### 3) 前端自动串行业务删除（先评论后文章）
文件：`Fronted/src/views/admin/ArticleList.vue`
```js
await removeCommentsByContent(row.id)
await removeContent(row.id)
ElMessage.success('评论和文章已删除')
```

## 七、验证结果

### 1) 前端构建
已执行并通过：
- `Fronted: npm run build` ✅

### 2) 后端编译
未完成编译验证，原因：
- 当前环境缺少 Java 运行环境变量：`JAVA_HOME` 未正确配置。

## 八、后续建议
1. 配置 `JAVA_HOME` 后执行 `Backend/mvnw.cmd -DskipTests compile`，完成后端编译校验。
2. 为分类/标签从 Redis 迁移到数据库表，增强持久化和可审计性。
3. 为文章编辑增加自动草稿与版本历史。
4. 增加管理端权限校验（接口级 + 按钮级）并接入登录态鉴权。
