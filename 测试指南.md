# 智能仪表盘测试指南

## 修复内容

1. **布局调整**：AI助手现在在右侧占据更大空间（16列），访问统计和快捷信息在左侧（8列）
2. **样式优化**：
   - AI助手区域使用渐变背景，更美观
   - Markdown渲染样式优化，标题、段落、列表都有更好的视觉效果
   - 统计卡片改为垂直布局，更适合窄列
   - 添加悬停效果和阴影
   - 自定义滚动条样式

3. **API路径修复**：
   - 后端Controller路径从 `/api/dashboard` 改为 `/dashboard`
   - 因为 `application.properties` 中已经配置了 `context-path=/api`
   - 实际访问路径：`http://localhost:12138/api/dashboard/assistant/report`

## 测试步骤

### 1. 启动后端
```bash
cd Backend
# 确保环境变量 DASHSCOPE_API_KEY 已设置
mvn spring-boot:run
```

### 2. 启动前端
```bash
cd Fronted
npm run dev
```

### 3. 访问页面
打开浏览器访问：`http://localhost:5173/admin/dashboard`

### 4. 测试功能

#### AI助手测试
1. 点击"重新生成"按钮
2. 观察是否出现流式输出效果（文字逐渐显示）
3. 检查Markdown格式是否正确渲染（标题、列表、加粗等）

#### 访问统计测试
1. 查看三个统计卡片是否正常显示
2. 查看热门页面列表是否显示

#### 快捷信息测试
1. 查看待审核评论、草稿箱等数字是否显示
2. 点击卡片测试跳转功能
3. 查看系统通知列表

## 可能的问题排查

### 问题1：AI助手没有内容
**原因**：千问API密钥未配置或无效
**解决**：
1. 检查环境变量 `DASHSCOPE_API_KEY` 是否设置
2. 查看后端控制台日志，确认是否有错误信息

### 问题2：访问统计数据为0
**原因**：当前返回的是模拟数据
**解决**：这是正常的，后续需要对接真实的统计服务

### 问题3：页面布局错乱
**原因**：浏览器缓存
**解决**：清除浏览器缓存或强制刷新（Ctrl+Shift+R）

### 问题4：404错误
**原因**：后端服务未启动或路径错误
**解决**：
1. 确认后端服务在 `http://localhost:12138` 运行
2. 访问 `http://localhost:12138/api/dashboard/statistics` 测试接口

## API端点

- AI助手报告（SSE）：`GET /api/dashboard/assistant/report`
- 访问统计：`GET /api/dashboard/statistics`
- 快捷信息：`GET /api/dashboard/quick-info`

## 下一步优化

1. 接入真实天气API
2. 对接Google Analytics或百度统计
3. 实现评论情感分析的准确度提升
4. 添加报告缓存机制
5. 支持自定义AI分析维度
